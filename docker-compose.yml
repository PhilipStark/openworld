version: '3.8'
services:
  openworld:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/server/openworld.db
    environment:
      - PORT=3001
      - WORLD_SIZE=50
      - TICK_INTERVAL=1500
    restart: unless-stopped
